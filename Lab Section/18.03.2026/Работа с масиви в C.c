Работа с масиви в C

Какво са масиви?
Масивите са структури от данни, които могат да съхраняват множество стойности от един и същи тип. Те са полезни за съхранение на големи 
количества данни и за извършване на операции върху тези данни. В C, масивите са дефинирани като последователност от елементи, които се съхраняват 
в непрекъсната памет. Всеки елемент в масива може да бъде достъпен чрез индекс, като първият елемент има индекс 0.

Деклариране на масив:

За да декларирате масив в C, трябва да посочите типа на данните, името на масива и броя на елементите, които ще съдържа. Синтаксисът е следният:
<тип_на_данните> <име_на_масива> <[брой_на_елементите]> ;
Например, за да декларирате масив от 10 цели числа, можете да използвате следния код:
int arr[10**];
** Задълнително трябва да посочим константа за броя на елементите в масива, тъй като размерът на масива трябва да бъде известен по време на компилация.

Инициализация на масив:
Масивите могат да бъдат инициализирани по време на декларирането им. Това може да стане чрез предоставяне на стойности в къдрави скоби {}. Например:
double balance[5] = {1000.0, 2.5, 3.5, 17.0, 50.0}; // Деклариране и инициализация на масив от 5 елемента

или с неопределен брой елементи, като се използва празни скоби:
int arr[] = {1, 2, 3, 4, 5};
double balance[] = {1000.0, 2.5, 3.5, 17.0, 50.0}; // Деклариране и инициализация на масив от 5 елемента

balance[4] = 70.0; // Промяна на стойността на петия елемент в масива

Друг начин за инициализация на масив е чрез използване на индексите на елементите. Например:
int a [3] = {1}; ///-> Масивът ще се състои от {1, 0, 0}

int a[3]={[0,1]=3}; ///-> Масивът ще се състои от {3, 3, 0}


Достъп до елементите на масива:
За да получите достъп до елементите на масива, използвайте индекса на елемента в квадратни скоби []. 
Индексите започват от 0, така че първият елемент има индекс 0, вторият елемент има индекс 1 и така нататък. 
Например:
int arr[5] = {1, 2, 3, 4, 5};
printf("%d", arr[0]); // Извежда първия елемент на масива, който е 1
printf("%d", arr[2]); // Извежда третия елемент на масива, който е 3
printf("%d", arr[4]); // Извежда петия елемент на масива, който е 5 

arr[5] -> *(arr + 5) -> *(5 + arr) -> 5[arr] // Всички тези изрази са еквивалентни и достъпват шестия елемент на масива, 
//който е извън границите на масива и може да доведе до неопределено поведение.

Масиви от символи (стрингове):

Масивите от символи, известни още като стрингове, са специален вид масиви, които съхраняват последователност от символи.
Стрингът в C е масив от символи, който завършва с нулев символ '\0', който служи като индикатор за края на стринга.
За да декларирате и инициализирате стринг, можете да използвате следния код:

char str[20] = "Hello, World!"; // Деклариране и инициализация на стринг
printf("%s", str); // Извежда стринга "Hello, World!"
Можете също така да използвате указатели за работа със стрингове. Например:
char *str = "Hello, World!"; // Деклариране и инициализация на стринг чрез указател
printf("%s", str); // Извежда стринга "Hello, World!"
Важно е да се отбележи, че когато използвате указатели за стрингове, те са константни и не могат да бъдат променяни.

char str[] = {'H', 'e', 'l', 'l', 'o', '!', '\0'}; // Деклариране и инициализация на стринг чрез масив от символи
printf("%s", str); // Извежда стринга "Hello"
В този пример, стрингът е инициализиран чрез масив от символи, като последният символ е нулев символ '\0', който указва края на стринга.

Заключение:
Масивите са мощен инструмент за съхранение и манипулиране на данни в C. Те позволяват съхранение на множество стойности от един и същи тип и предоставят лесен 
начин за достъп до тези стойности чрез индекси.
Работата с масиви изисква внимание, особено при достъп до елементите, за да се избегне извън границите на масива, което може да доведе до неопределено поведение.
При работа с масиви от символи (стрингове), е важно да се помни, че те завършват с нулев символ '\0', който указва края на стринга. Това е от съществено 
значение за правилното функциониране на функциите за работа със стрингове в C.

Присвояване стойност на низ:
Четене на цял ред:
#inclue <stdio.h>
int main() {
    char str[100];
    printf("Enter a string: ");
    fgets(str, sizeof(str), stdin); // Чете цял ред от входа и го съхранява в str
    printf("You entered: "); // Извежда въведения низ
    puts(str); // Извежда въведения низ
    return 0;
}

Четене на стойност от клавиатурата:
#include <stdio.h>
int main() {
    char str[100];
    printf("Enter a string: ");
    scanf("%s", str); // Чете низ от входа и го съхранява в str (до първия интервал)
    printf("You entered: %s", str); // Извежда въведения низ
    return 0;
}

Често използвани функции за работа със стрингове:
strcpy(s1, s2) - Копира съдържанието на стринг s2 в стринг s1.
strncat(s1, s2) - Добавя съдържанието на стринг s2 към края на стринг s1, като се ограничава до определен брой символи.
strlen(s1) - Връща дължината на стринг s, без да включва нулевия символ '\0'.
strcmp(s1, s2) - Сравнява два стринга s1 и s2. Връща 0, ако са равни, отрицателно число, ако s1 е по-малък от s2, и положително число, ако s1 е по-голям от s2.
strchr(s, c) - Връща указател към първото срещане на символ c в стринг s, или NULL, ако символът не е намерен.
strstr(s1, s2) - Връща указател към първото срещане на подстринг s2 в стринг s1, или NULL, ако подстрингът не е намерен.
... и други: http://www.cplusplus.com/reference/cstring/