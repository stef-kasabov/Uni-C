Динамични масиви в C

Какво представя динамичният масив?
Динамичният масив е структура от данни, която може да променя размера си по време на изпълнение на програмата. 
Това означава, че можете да добавяте или премахвате елементи от масива без да се налага да създавате нов масив и да копирате старите данни. 
Динамичните масиви обикновено се реализират с помощта на указатели и функции за динамично разпределение на паметта, като malloc() и free() в C.

Как да създадем динамичен масив в C?
За да създадете динамичен масив в C, можете да използвате функцията malloc() за разпределение на памет.

Ето пример за създаване на динамичен масив от цели числа:

#include <stdio.h>
#include <stdlib.h> // за malloc() и free()
int main() {
    int n; // брой елементи в масива
    printf("Enter the number of elements: ");
    scanf("%d", &n);

    // Създаване на динамичен масив от цели числа
    int *array = (int *)malloc(n * sizeof(int));
    
    // Проверка дали паметта е успешно разпределена
    if (array == NULL) {
        printf("Error allocating memory!\n");
        return 1; // изход с грешка
    }

    // Въвеждане на елементи в масива
    printf("Enter %d integers:\n", n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &array[i]);
    }

    // Извеждане на елементите от масива
    printf("The entered numbers are:\n");
    for (int i = 0; i < n; i++) {
        printf("%d ", array[i]);
    }
    printf("\n");

    // Освобождаване на заделената памет
    free(array);
    
    return 0; // успешен изход
}

Функции за заделяне и освобождаване на памет:
- malloc(): заделя памет за динамичен масив
- free(): освобождава заделената памет
- realloc(): променя размера на вече заделената памет
- calloc(): заделя памет и я инициализира с нули

Те имат следния вид:
void *malloc(size_t byte_size);
void *calloc(size_t elements, size_t byte_per_element);
void *realloc(void *prev_address, size_t new_size);
void free(void *array);

void* - безтипов указател, който може да бъде преобразуван към всякакъв тип указател.
size_t - тип данни, който представлява размера на паметта в байтове.

- Функцията malloc() заделя памет от посочения размер и връща указател към началото на тази памет. Ако заделянето на паметта не е успешно, тя връща NULL. 
malloc() НЕ изтрива данните, а само ги заделя, така че съдържанието на заделената памет може да бъде неинициализирано.

- Функцията calloc(,) заделя памет за определен брой елементи от определен размер и инициализира всички байтове в тази памет с нули. 
Ако заделянето на паметта не е успешно, тя връща NULL.

- Функцията realloc(,) променя размера на вече заделената памет. Ако новият размер е по-голям от стария, съдържанието на старата памет се копира в новата памет,
 а останалата част от новата памет остава неинициализирана.

- Функцията free() освобождава заделената памет, която вече не е необходима. След извикване на free(), указателят към тази памет става невалиден и 
 не трябва да се използва.